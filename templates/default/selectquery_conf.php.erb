<?php if (!defined('PmWiki')) exit();
## Generated by Chef from selectquery_conf.php.erb ##

# Run a SQL SELECT query based on parameters submitted

<% 
   sq = node['pmwiki']['selectquery']
   if sq['page_group'] then 
%>
if (FmtPageName('$Group', $pagename) == '<%= sq['page_group'] %>' {
  include_once("$FarmD/cookbook/<%= sq['script'] %>.php");
}
<% else %>
include_once("$FarmD/cookbook/<%= sq['script'] %>.php");
<% end %>

<% raise "Must have a value for node['pmwiki']['selectquery']['database']" unless sq['database'] %>
$SelectQuerySettings['database'] = "<%= sq['database'] %>";
<% if sq['seperator'] then %>
$SelectQuerySettings['Seperator'] = "<%= sq['seperator'] %>";
<% end %>
<% if sq['seperator'] then %>
$SelectQuerySettings['FieldID'] = "<%= sq['field_id'] %>";
<% end %>
<% if sq['seperator'] then %>
$SelectQuerySettings['TableID'] = "<%= sq['table_id'] %>";
<% end %>

