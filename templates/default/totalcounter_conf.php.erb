<?php if (!defined('PmWiki')) exit();
## Generated by Chef from selectquery_conf.php.erb ##

# A statistic counter - counts page views, users, languages, browsers, 
# operating systems, referers, locations and web bots

<% 
   tc = node['pmwiki']['totalcounter']
%>

<% if tc['action'] then %>
$TotalCounterAction = '<%= tc['action'] %>';
<% end %>
<% if tc['auth_level'] then %>
$TotalCounterAuthLevel = '<%= tc['auth_level'] %>';
<% end %>
<% if tc['max_items'] then %>
$TotalCounterMaxItems = <%= tc['max_items'] %>;
<% end %>
<% if tc['enable_lookup'] then %>
$TotalCounterMaxItems = 1;
<% end %>
<% tc['blacklist'].each do |key, ray| 
   unless ray.empty? then %>
$TotalCounterBlacklist['<%= key =%>'] = array(
<%   ray.each do |str| %>
        '<%= str %>', 
<%   end %>
);
<% end %>
<% if tc['bar_color'] then %>
$TotalCounterBarColor = '<%= tc['bar_color'] %>';
<% end %>
<% unless tc['show_numbers'] then %>
$TotalCounterShowNumbers = 0;
<% end %>



include_once("$FarmD/cookbook/<%= tc['script'] %>.php");
